[project]
name = "babylm-2026"
version = "0.1.0"
description = "BabyLM 2026 Challenge submission — TUM LLM Practical Course"
requires-python = ">=3.11"
authors = [
    { name = "Deep Pambhar" },
    { name = "Alessio Piroli" },
    { name = "Nour Hadjfredj" },
]
dependencies = [
    # Add as the project grows. Likely candidates:
    # "torch",
    # "transformers",
    # "datasets",
    # "accelerate",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
